# Product Requirements Document

## Working directory

`src/`

## Package manager

pnpm

## Tasks

[ ] Get a list of all the dependencies that can be updated. Add every dependency update as a task to this .ralph/PRD.md. Update the dependencies one by one. Read the changelogs to see if we should update something in the code. Update the dependency and if needed, our code. Lint, lint & check types. Fix errors. Commit.
